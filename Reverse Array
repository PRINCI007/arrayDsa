class Main{
  public static void main(){
    int arr[]= {7,8,5,6,2,1};
    for(int i=0, j= arr.length-1; i<j; i++,j--){
      arr[i]=arr[i]+arr[j];
      arr[j]=arr[i]-arr[j];
      arr[i]=arr[i]-arr[j];
    }

    for(int k=0;k<arr.length;k++){
      Sastem.out.print(arr[k]+ " ");
    }

    
  }
}

// this solution is a in-place solution, the time-complexity is O(n) and the space-complexity is 0(1).
